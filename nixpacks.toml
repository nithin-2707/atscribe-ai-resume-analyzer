[phases.setup]
nixpkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "cd backend",
  "npm install --production=false"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend && node server.js"
